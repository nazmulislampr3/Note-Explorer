{
  "version": 2,
  "builds": [
    {
      "src": "./index.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["**/*.ts", "package.json", "tsconfig.json"] }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./index.ts"
    }
  ]
}
